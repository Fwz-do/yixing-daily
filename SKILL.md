---
name: yixing-daily-report
description: 宜兴市重点企业商业动态日报生成与发布工具。每天更新固定URL的日报页面（当日动态+历史归档+密码门+二维码封面），内容覆盖53家宜兴本地/上市重点企业最新动态。触发词："更新宜兴日报"、"生成今天的宜兴企业动态"、"宜兴日报"、"yixing daily"。
---

# 宜兴市重点企业商业动态日报

给宜兴商务局做的付费日报产品。每天更新同一个 URL 的内容（不换链接），密码门保护，微信扫二维码直达。本 skill 文件夹自包含全部所需资料（企业库、模板、样式规范、部署脚本），复制到任意智能体即可直接产出与当前线上版本一致的结果。

## 文件清单

```
yixing-daily-report/
├── SKILL.md                              ← 本文件，完整工作流程
├── references/
│   ├── day-report-template.html          ← 日报完整HTML模板（含真实结构，照抄改数据即可）
│   ├── qr-cover-template.html            ← 二维码封面卡模板（620×800px）
│   ├── keyword-monitoring-dashboard.html ← 关键词监测大屏（配套展示页，见下方说明）
│   ├── source-funnel-battlemap.html      ← 信源漏斗作战图（配套展示页，验真方法论出处）
│   └── style-guide.md                    ← 配色/结构/文案长度/部署规范
├── assets/
│   └── enterprise-database.json          ← 53家监测企业底库（本地31+上市22，含股票代码）
└── scripts/
    └── deploy.sh                         ← 一键部署到线上固定URL
```

## 三个线上页面的关系（重要，不要混淆）

宜兴商务局这个产品对客户展示的是**三个互相配套的页面**，日报只是其中每天更新的一个：

| 页面 | URL | 作用 | 更新频率 |
|---|---|---|---|
| **① 每日商业动态日报**（本skill主产出） | `https://api.lexoavatar.com/pages/yixing-daily-v2-20260730.html` | 客户实际每天扫码看的内容——当日动态卡片 + 历史归档，是**交付给客户的正文产品** | **每天更新**（本skill核心任务） |
| **② 关键词监测大屏** | `https://api.lexoavatar.com/pages/yixing-keyword-dashboard-20260730.html` | 深蓝科技风数据大屏，展示"我们在监测什么"——53家企业底库、关键词分类、KPI统计、信源状态。用于**向客户证明系统的监测能力和覆盖面**，是销售/演示向的展示页，不是日报正文 | 底库/统计口径变化时更新，**不需要每天跑** |
| **③ 信源漏斗作战图** | `https://api.lexoavatar.com/pages/yixing-source-funnel-20260730.html` | 展示信源可信度分层（L1官方权威→L2主流财经→L3产业垂直→L4本地/公众号）+ 取源与研判铁律。用于**向客户证明日报里的每条内容是怎么把关验真的**，是方法论展示页，不是日报正文 | 方法论调整时更新，**不需要每天跑** |

②③ 两个页面不参与每日自动更新流程，但它们是本 skill 的重要配套资料——**日报正文（①）里承诺的"可追溯来源""对宜启示"这些规则，其权威出处就是③里写的"取源与研判铁律"**，两者必须口径一致。如果 SKILL.md 里的验真规则以后要改，③页面上的文字说明也要同步改，不能各说各话。

### 信源可信度四级分层（来自 source-funnel-battlemap.html，验真铁律的方法论依据）
- **L1 🏛️ 官方权威**：政府/交易所/海关/证监 —— 最可信，优先取用
- **L2 📈 主流财经**：巨潮资讯/沪深交易所/证券时报/东财/同花顺
- **L3 🔬 产业垂直**：集微网/SMM/动脉网/北极星 —— 按赛道深挖
- **L4 📰 本地+公众号**：一手动态素材 —— **必须回链L1/L2权威源核实后才能入库**，不能单独作为唯一信源

取源与研判五条铁律：① 分层取用，冲突时以高层级为准 ② 双源验证——金额/产能/外资等关键数字至少2个独立信源交叉一致，否则标「待核实」③ 必标来源——每条动态末尾附可点击来源链接+日期 ④ 企业核名——以企查查/天眼查全称为准，杜绝简称歧义（如"远东"要明确是远东股份还是远东电缆）⑤ 对宜落点——每条动态附一句"对宜启示"

## 🔴 验真铁律（最高优先级，高于条数/高于deadline）

这是宜兴商务局的**付费政务产品**，任何一条虚假/编造信息一旦被发现，砸的是信任而不是一次失误。执行时必须做到：

- **每条事实必须来自真实抓取到的网页内容**，不能凭训练知识/常识"推断出"看起来合理的新闻（尤其注意：模型训练知识有截止日期，当天日期之后的"新闻"如果不是刚搜到的，100%是编造的，绝对不能写）
- **每个 `url` 字段必须是搜索/抓取过程中真实拿到的链接**，不能编、不能猜格式（比如按"东方财富文章URL大概长这样"去拼一个不存在的链接）
- **正文里的数字（涨跌幅、金额、日期、同比数据）必须原样摘自信源**，不能自己算/自己估/自己"四舍五入编圆"
- 抓取到的网页内容如果和标题对不上、或者页面打不开/404、或者内容与主题不相关 → **这条直接放弃，不能将就着写**
- **双源验证**：涉及金额/产能/外资等关键数字，至少2个独立信源交叉一致才能写实数，否则标注"待核实"或换一条更有把握的动态
- **企业核名**：企业名以企查查/天眼查全称为准，杜绝简称歧义（如"远东"必须明确是远东股份还是远东电缆，两家不是一家）
- **信源优先级参考上方"信源可信度四级分层"**：优先取L1/L2，L4本地/公众号线索必须回链L1/L2核实后才能入库
- **宁可条数少，不能有一条假的**。真实动态如果只搜到8条能验真的，就发8条，不要为了凑够12-18条硬编或夸大次要信息
- 执行流程第2步"搜集当日动态"完成后，发布前必须过一遍**自查清单**：逐条问自己"这条我是不是真的抓取到了这个网页、这段话是不是网页里原话/原意，还是我自己编的？"——答不上来的条目直接删
- 发给财财的汇报里，如果本轮验真后条数明显低于往常（比如个位数），如实说明"今天验真通过的动态较少"，不要为了数字好看硬凑

## 🔴 时效性铁律（和验真铁律同等优先级）

日报卖的是"新鲜"，不是"存档"。当日动态里的每一条，新闻/公告的**实际发布日期必须是 t（当天）或 t-1（前一天）**，最多放宽到 t-2，绝不能收录更早的旧闻凑数：

- 搜到的动态先看发布日期，**超过 t-2 的一律不用**，哪怕内容再精彩、再对宜兴有意义，也要放弃或换一条更新的
- 同一企业如果只搜到过时的旧新闻，宁可这家企业今天不报，也不要为了"每家都露个面"硬塞旧闻
- 卡片上的 `date` 字段必须是真实发布日期（不是抓取当天的日期），前端会显示这个日期，滞后的内容会被客户一眼看穿
- 历史动态（HISTORY）不受此限制——那里本来就是存档，可以有更早的日期；**只有"当日动态"三个Tab要求严格新鲜**
- 自查提醒：写完卡片后，逐条检查 `date` 是不是真的落在 t 或 t-1（最多 t-2），不是的立刻删除换新的一条

## 核心事实（铁律，不要凭记忆改）

- **线上固定URL**：`https://api.lexoavatar.com/pages/yixing-daily-v2-20260730.html`（文件名带的日期是历史遗留，**永远不变**——每天只更新文件内容，不换文件名/URL，这样二维码和收藏链接永远有效）
- **部署路径**：`/var/www/html/pages/yixing-daily-v2-20260730.html`
- **密码**：`123456`（用户明确要求改才改）
- **卡片四层结构，不可变**：企业名称 → 主旨(gist) → 正文(body-text) → 对宜启示(rel)
- **当日内容必须与历史动态零重复**——同一企业同一事件不能当天又出现在历史里
- **当日动态不显示搜索筛选栏；只有切到历史动态才显示**（这是客户明确要求过的规则，别做反了）
- 详细视觉/结构规范见 `references/style-guide.md`

## 执行流程（每日一次，完整跑通约需10-20分钟）

### 1. 确认日期与读取现状

```bash
date +%Y-%m-%d   # 绝不凭感知写日期
curl -s https://api.lexoavatar.com/pages/yixing-daily-v2-20260730.html -o /tmp/yx_live.html
```
从 `/tmp/yx_live.html` 里提取当前的 `DATA_LOCAL` / `DATA_CHAIN` / `DATA_TRACK` / `HISTORY` 四个数组——这是"今天"要处理的基准数据（今天要归档的是这份文件里的"昨天当日动态"，历史数组要在这基础上追加，不能用 references 目录里的旧模板当基准，那只是结构参考）。

### 2. 搜集当日真实动态（避免与历史重复）

- 目标：覆盖 `assets/enterprise-database.json` 里的53家企业（本地31+上市22），只找 **公告/新闻发布日期在 t 或 t-1（最多放宽到 t-2）** 的真实事件（业绩、涨跌停、中标、融资、投产、合作、监管等）——详见下方"时效性铁律"，超过t-2的旧闻一律不收
- 逐条核对：company + 关键词 + 日期，优先信源：交易所公告、新华社/央媒、证券时报/东方财富/财中社等权威财经媒体、企业官网
- **对照第1步取出的 `HISTORY` 数组去重**——同一企业同一事件不能再报一次；同一企业若有新进展（如"股价异动"→"公司澄清"）可以算新条目
- 建议目标条数：本地/产业链/赛道三类合计 12-18 条（参考历史每日约14条的量级），不追求覆盖全部53家，只报有实质新闻的
- **条数是参考不是指标**——验真铁律优先于条数，真实能验证的有多少就发多少，绝不为凑数编造（详见上方"验真铁律"章节）
- 可用 WebSearch/web_search 并发搜索，每家企业查一次最新动态即可，不需要逐条深挖

### 3. 分类到三个 Tab（这是新闻主题分类，不是数据库固定属性）

- **📍 本地企业**：偏本地视角的经营/资本动态（回购、并购、股价、投产等），不强制要求属于"本地31家"名单，只要是宜兴关联企业即可
- **🔗 3+3+N 产业链**：能明确归入宜兴"3+3+N"产业框架的新闻，按 `enterprise-database.json` 里 `industries` 字段的 `chainTier` 分到 tier1/tier2/tier3
- **🎯 重点赛道**：能打上跨行业热点赛道标签的新闻（如 AI算力、固态电池、光伏去银化、储能出海等），赛道名自定，不是固定列表

### 4. 写卡片文案（四层结构，长度要求见 style-guide.md）

- `title`（gist）：一句话主旨，20-40字
- `summary`（body-text）：完整事实，含数据/日期/来源，120-220字
- `rel`：**对宜启示**，不是复述新闻，要写"这件事对宜兴意味着什么"——产业链联动、招商信号、风险提示，100-180字
- 每条务必带真实 `url`（来源链接可点击验证），`src`（来源媒体名），`date`（MM-DD）

### 5. 归档：把"昨天的当日动态"精简后追加进 HISTORY

- 把第1步读到的旧 `DATA_LOCAL+DATA_CHAIN+DATA_TRACK`（也就是"昨天"），每条精简成 `{cdate, co, ind, t1, title(≤20字), rel(≤15字), src, url}` 格式，追加到 `HISTORY` 数组最前面（新的在前，或保持原数组顺序追加均可，前端会按cdate分组展示）
- 原有的 `HISTORY` 数组内容原样保留，不要删除更早的历史

### 6. 生成新的日报HTML

- 用 `references/day-report-template.html` 的**完整结构**（CSS/密码门/JS函数不动），只替换：
  - `<div class="date">2026-07-31</div>` → 今天日期
  - `<meta property="og:title">` 里的日期
  - `.pgfoot` 里的日期
  - `KEY="yx_daily_0730_ok"` → 按今天日期改（如 `yx_daily_0731_ok`），否则密码门会因为 sessionStorage 复用而在新的一天被跳过
  - `DATA_LOCAL` / `DATA_CHAIN` / `DATA_TRACK` 三个数组 → 今天新写的内容
  - `HISTORY` 数组 → 第5步归档后的完整历史
  - `chipN` 会自动 = `allData.length`，不用手动改
  - Excel导出文件名可选更新为当天日期，非必需
- 生成后本地起一个 `<script>` 语法检查（如 `node -c` 或用浏览器打开控制台看报错），确认三个Tab渲染无误、密码门正常、历史动态搜索筛选正常，再部署

### 7. 部署

```bash
bash scripts/deploy.sh /path/to/生成好的新日报.html
```
脚本会自动备份线上旧版本再覆盖，URL 不变。**部署后务必实测**（用 browser 工具打开线上URL验证）：输入密码123456能进、三个Tab内容不同、历史动态条数增加、切换自动回顶部。

### 8. 生成当日二维码封面

- 用 `references/qr-cover-template.html`，替换：`.date` 文本、`.stat`（共N条重点动态）、`QRCode(...)` 的 `text` 参数（指向今天部署好的URL，通常URL不变可以保持一致）
- 用 OpenClaw `browser` 工具打开该HTML（可以是本地文件路径或临时上传的可访问URL），截图导出PNG（620×800，等待QR脚本渲染完成，`document.title` 会变成 `"READY"` 可作为渲染完成信号）

### 9. 发送

- 通过飞书把二维码图片 + 简短文案发给指定接收人（默认：财财 `ou_9d02bb7564d2f036c482ffb5f7a05fa1`，isolated cron session **必须显式带 channel+target**）
- 文案模板：「宜兴日报已更新 ✅ 今天N条新动态，历史动态累计M条。扫码查看（密码123456）」

## Cron 定时（硬性DEADLINE：8:30前财财必须收到）

客户要求**8:30(北京/香港时间，同为UTC+8)扫码就要看到当天更新**，不是8:30才开始跑。已注册 cron job `yixing-daily-report`，`0 45 7 * * *` @ `Asia/Hong_Kong`（**7:45开始跑，留45分钟缓冲**），`session=main`，payload 里显式写明8:30硬截止、卡住时优先精简条目也要按时发送。如需迁移到其他智能体，用 `openclaw cron add` 参考本机配置重新注册，注意：
- 目标智能体要能访问 `/var/www/html/pages/`（同一台服务器）或替换成对应的部署路径
- **务必保留提前量**，不要把 cron 时间设成用户要求收到的时间本身——研究+写作+部署+验证有不确定性，需要缓冲

## 常见坑

- 忘记改 `sessionStorage` key 里的日期 → 用户当天首次打开会因为浏览器缓存了前一天的"已登录"状态而跳过密码门（不算大问题，但下次改结构时留意）
- HTML `head`/`body` 用 sed/grep 分段拼接容易把 `<script>` 标签或 `dayView`/`histView` 的容器 div 弄丢——**优先整体替换数组内容，不要重新拼接HTML骨架**
- 历史动态条数会一直累积增长，目前（2026-07-31）已到32条，无需清理/分页，前端按日期分组即可
- 股票代码以最新核实为准：泛亚环保是 `00556.HK`，不是之前误记的 `01371.HK`
